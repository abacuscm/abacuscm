#ifndef __MARKER_H__
#define __MARKER_H__

#include <string>

class Marker {
protected:
	Buffer getFile(std::string attrname);
	std::string attribute
public:
	Marker();
	virtual ~Marker();
	
	virtual Result mark(
};

#endif
